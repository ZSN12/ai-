
# 电商AI客服转人工质检机器人

基于规则引擎 + LLM 语义兜底 + TF-IDF 政策检索的智能质检系统，自动分析转人工会话的根本原因，生成可视化报告。

## 📖 项目简介

本系统专为跨境电商（如 TikTok Shop、抖音小店）AI 客服团队设计。通过解析聊天记录，自动识别客户要求转人工的真实原因（共 10 类），并关联企业内部政策条款，最终输出 Excel 质检报告和日趋势图表。

### 核心能力
- ✅ 支持中、英、菲律宾三语关键词匹配
- ✅ 规则引擎优先，LLM（DeepSeek）兜底，兼顾速度与准确率
- ✅ 店铺政策语义检索（TF-IDF），自动匹配相关条款
- ✅ 动态并发控制，高效处理万级会话
- ✅ 大文件自动预警，流式估算行数
- ✅ 一键导出 Excel 报告 + 运行日志

---

## 🚀 快速开始

### 1. 环境要求
- Python 3.8 或更高版本
- 建议使用虚拟环境（venv / conda）

### 2. 安装依赖
pip install -r requirements.txt

### 3. 配置 API Key
在项目根目录创建 `.env` 文件，填入你的 DeepSeek API Key：
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
LLM_MAX_CONCURRENT=5    # 可选，LLM 最大并发数
```

### 4. 启动应用
```bash
streamlit run app.py
```
浏览器将自动打开 `http://localhost:8501`。

---

## 📂 输入文件格式

### 会话记录文件（必填）
支持 CSV 或 Excel（.xlsx / .xls），必须包含以下列：

| 列名 | 说明 | 示例 |
|------|------|------|
| `会话ID` | 唯一标识一个会话 | `abc123` |
| `Message` | 消息文本内容 | `我的订单还没收到` |
| `Type` | 消息类型（整数） | `1`（客户）、`42`（转人工理由）等 |
| `Timestamp` | 消息时间戳 | `2024-01-15 10:23:45` |

> ⚠️ 注意：系统内部使用固定的 `Type` 编码，请确保与文档一致（见附录）。

### 店铺政策文件（可选）
若需自动匹配店铺政策，可上传包含以下列的 Excel/CSV：

| 列名 | 说明 |
|------|------|
| `Topic` | 政策标题/编号 |
| `Content` | 政策详细内容 |
| `Keywords` | 附加关键词（可选） |

---

## 🎮 使用流程

1. **上传文件**
   - 左侧上传会话记录（必填）
   - 右侧上传政策文件（可选）

2. **开始质检**
   - 点击 `▶️ 开始质检` 按钮，系统将自动分析所有会话

3. **查看结果**
   - 📊 **分类统计**：各转人工类别的数量与占比
   - 📈 **日趋势图**：各类别按天的变化趋势（需 `Timestamp` 列）
   - 📄 **质检明细**：每个会话的分类结果、触发原因、匹配的政策条款
   - 📝 **运行日志**：实时处理状态（默认折叠，点击展开）

4. **下载报告**
   - 点击 `📥 下载质检报告` 获取 Excel 文件（含明细和统计两个 Sheet）
   - 点击 `📋 下载运行日志` 导出完整日志（用于排查问题）

---

## 🧠 分类体系说明

| 类别名称 | 触发典型场景 |
|----------|--------------|
| 售后退款退货 | 客户要求退款、退货、换货、补发 |
| 物流 | 催单、查物流、丢件、错发漏发 |
| 商品质量问题 | 破损、瑕疵、功能故障 |
| 修改订单信息 | 改地址、改尺码、合并订单 |
| 定制服务 | 印字、印号、定制图案 |
| 商品信息不足 | 问尺寸、材质、库存 |
| 客户要求 | 直接说“转人工”“找人工” |
| 店铺政策要求 | 系统设置“无售后”等政策导致转人工 |
| AI关闭直接转人工 | AI 接待功能被关闭 |
| 未知 | 无法自动判断的会话 |

> 规则引擎优先级：售后退款退货 > 物流 > 商品质量问题 > 修改订单信息 > 定制服务 > 商品信息不足 > 客户要求 > 店铺政策要求 > AI关闭直接转人工

---

## 📁 项目结构

```
.
├── app.py                 # Streamlit 主界面
├── core.py                # 业务核心：规则引擎、LLM、政策检索、并发处理
├── config.py              # 配置管理（Pydantic Settings）
├── utils.py               # 工具函数：文件读取、行数估算
├── prompts.py             # LLM 提示词与多语言关键词库
├── logger_config.py       # 日志持久化配置
├── requirements.txt       # 项目依赖
├── 使用手册              # 使用手册
├── tests/                 # 单元测试
│   └── test_core.py
└── logs/                  # 运行日志目录（自动生成）
```

---

## 🔧 常见问题

**Q1：上传文件后提示“缺少必需列”怎么办？**
A：请确保你的文件包含 `会话ID`、`Message`、`Type` 三列，列名严格一致。

**Q2：为什么折线图没有显示？**
A：需要数据中包含有效的 `Timestamp` 列，且至少有两个不同日期的会话。

**Q3：政策匹配只对“店铺政策要求”生效吗？**
A：当前版本默认仅对该类别匹配政策。如需扩展，可在 `core.py` 的 `match_policies` 函数中修改判断条件。

**Q4：LLM 调用失败怎么办？**
A：检查 `.env` 中的 `DEEPSEEK_API_KEY` 是否正确，网络是否能访问 `api.deepseek.com`。系统会自动降级为规则引擎结果。

**Q5：运行很慢或内存不足？**
A：对于超过 10 万行的大文件，建议拆分成多个小文件后分批处理。未来版本将支持流式分组以降低内存压力。

---

## 📎 附录：消息 Type 编码对照表

| Type | 含义 |
|------|------|
| 1 | 客户消息 |
| 2 | AI（抖音） |
| 5 | 人工客服（抖音） |
| 19 | AI（TikTok） |
| 20 | AI 系统（TikTok） |
| 29 | 人工客服（TikTok） |
| 41 | 系统欢迎语 |
| 42 | 转人工理由 |
| 44 | AI 接待关闭 |

---
张少楠
最后更新：2026-04-13